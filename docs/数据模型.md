# 数据模型

## Root
- `schemaVersion: number`，当前为 `8`；表示前端状态结构版本。
- `groups: Group[]`
- `lists: List[]`
- `taskGroups: TaskGroup[]`，清单内任务分组。
- `tasks: Task[]`
- `trash: Task[]`
- `listTrash: ListTrash[]`
- `viewOrders: Record<string, string[]>`，普通任务视图的用户拖拽顺序；key 包括 `today`、`inbox`、`planned`、`important`、`list:<id>`、`search:<query>`。
- `profile: Profile`，当前设备的本地个人资料；旧数据缺失时按默认值补齐。
- `settings: Settings`

## 存储位置
- `npm run dev` 使用 Tauri 开发模式，主数据存在 Tauri 应用数据目录：Windows 为 `%APPDATA%\cn.duqimeng.simpletodo\simpletodo.db`。
- 图片附件不写入数据库 BLOB，存放在同目录下的 `attachments/` 文件夹，数据库只保存附件元数据和相对路径。
- 只有单独运行 `npm run frontend:dev` 并在浏览器中打开页面时，才使用 `localStorage` 的 `simple-to-do:data` 作为前端调试兜底。

## Group
- `id: string`
- `name: string`
- `collapsed: boolean`
- `sortOrder: number`，侧栏分组手动排序权重。

## Profile
- `id: string`，本机稳定 UUID；创建后不因登录、退出登录或绑定云端身份而改变。
- `nickname: string`，去除首尾空白后为 1–24 个字符；默认 `"易简用户"`。
- `avatarRelativePath: string | null`，自定义头像相对 `profile/avatars/` 的路径；`null` 时使用默认头像。
- `avatarSha256: string | null`，自定义头像内容 hash；与路径一起用于去重和安全读取。
- `avatarUpdatedAt: string | null`，最后更新头像的 ISO 时间。
- `accountId: string | null`，预留的远端账户映射；当前本地模式必须为 `null`，不可作为本机 Profile 的主键。
- `createdAt: string`
- `updatedAt: string`

默认头像不保存为图片文件：由 `nickname` 的首个可见字符和稳定配色在运行时生成。头像显示区应优先展示昵称，不能把未登录的本机 Profile 标记为“账号”。

## List
- `id: string`
- `name: string`
- `groupId: string | null`，所属侧栏分组ID，null 表示未分组。
- `color: string`
- `isSystem: boolean`
- `sortOrder: number`，侧栏清单手动排序权重。
- `viewMode: "list" | "group"`，任务显示模式，默认 `"list"`。列表模式平铺显示所有任务，分组模式按 TaskGroup 组织显示。

## ListTrash
- `id: string`，原清单 ID。
- `name: string`
- `groupId: string | null`
- `color: string`
- `isSystem: false`
- `sortOrder: number`
- `deleted: true`
- `deletedAt: string`
- `taskCount: number`，删除时清单内任务数量。

## TaskGroup（任务分组）
- `id: string`，分组唯一标识。
- `name: string`，分组名称，如"进行中"、"待审核"。
- `emoji: string`，可选的分组图标。
- `color: "auto" | "accent" | "blue" | "violet" | "amber" | "rose" | "green" | "cyan" | "coral" | "indigo" | "teal" | "brick" | "custom"`，分组背景色；`auto` 根据分组名称与 ID 稳定选择协调色板。
- `customColor: string`，仅 `color="custom"` 时生效，格式为 `#RRGGBB`；缺失或无效时默认 `#5B8DEF`，兼容旧 JSON。
- `listId: string`，所属清单ID，每个清单可有多个任务分组。
- `sortOrder: number`，分组在清单内的排序权重，数值越小越靠前。
- `collapsed: boolean`，分组模式下是否折叠，默认 `false`。
- `createdAt: string`，创建时间 ISO 格式。
- `updatedAt: string`，最后修改时间 ISO 格式。

## Task
- `id: string`
- `title: string`
- `description: string`，兼容旧纯文本描述。
- `descriptionHtml: string`，富文本备注 HTML；备注图片仅保存 `attachments/...` 相对路径，不保存 base64 预览数据。旧备注中的 base64 图片会在打开编辑器时导入附件目录并自动替换为路径引用。
- `completed: boolean`
- `completedAt: string | null`
- `deleted: boolean`
- `deletedAt: string | null`
- `deletedByListId: string | null`，因删除清单进入垃圾桶时记录来源清单。
- `pinned: boolean`
- `important: boolean`
- `myDayDate: string | null`，格式为 `YYYY-MM-DD`，表示加入某天的今日聚焦。
- `listId: string`
- `taskGroupId: string | null`，所属任务分组ID，null 表示未分组；仅在清单的 `viewMode="group"` 时使用。
- `dueDate: string | null`
- `reminderAt: string | null`
- `reminderDisabled: boolean`，用户是否明确关闭该任务的提醒；默认 `false`，有截止日期时默认以截止时间作为提醒时间。
- `reminderNotifiedAt: string | null`，最近一次已成功投递该提醒的时间；修改截止日期或提醒时间时重置为 `null`，用于启动后的补发与去重。
- `repeatRule: "daily" | "weekly" | "monthly" | "yearly" | string | null`
- `priority: 0 | 1 | 2 | 3`
- `tags: string[]`
- `subtasks: Subtask[]`
- `attachments: Attachment[]`
- `comments: Comment[]`，仅保留旧数据兼容，当前 UI 不展示评论入口。
- `editorMode: string`，兼容旧模式字段，当前 UI 不再使用模式切换。
- `createdAt: string`
- `updatedAt: string`

## Subtask
- `id: string`
- `title: string`
- `completed: boolean`
- `createdAt: string`
- `completedAt: string | null`
- `sortOrder: number`，子任务手动排序权重。

## Attachment
- `id: string`
- `kind: "image" | "file" | "unknown"`，附件类型。
- `mime: string`，MIME 类型。
- `originalName: string`，原始文件名，用于 UI 展示。
- `path: string`，旧字段兼容，保留原始路径或历史文件名。
- `relativePath: string`，相对 `attachments/` 的文件路径，例如 `images/2026/07/{sha256}.png`。
- `sha256: string`，文件内容 hash，用于去重。
- `sizeBytes: number`，文件大小。
- `width: number | null`，图片宽度。
- `height: number | null`，图片高度。
- `url: string`，运行时预览地址；外置附件保存时不写入该字段。
- `createdAt: string`
- `lastReferencedAt: string`

## Settings
- `theme: "mint" | "blue" | "violet" | "graphite"`
- `themeBackgrounds: boolean`，是否让三栏背景随主题色生成低饱和层次，默认 `false`。
- `density: "comfortable" | "compact"`
- `detailOpen: boolean`
- `detailWidth: number`，详情面板宽度，范围 320-800px，默认 430px。
- `startView: "today" | "inbox" | "planned" | "important"`
- `completedVisible: boolean`
- `groupCompletedDisplayMode: "in-group" | "separate-section"`，分组模式下已完成任务保留在原分组或集中到列表底部。
- `groupCompletedVisibleByDefault: boolean`，进入分组模式时是否默认显示已完成任务。
- `trashRetentionDays: number`，垃圾桶保留天数，默认 30 天。
- `soundEnabled: boolean`，操作音效总开关。
- `soundTaskEnabled: boolean`，任务操作音效开关。
- `soundListEnabled: boolean`，清单操作音效开关。
- `soundGroupEnabled: boolean`，分组操作音效开关。
- `soundDragEnabled: boolean`，拖动排序的指示线音效开关，默认 `true`。
- `reminderNotificationsEnabled: boolean`，系统提醒通知开关。
- `reminderSoundEnabled: boolean`，系统提醒声音开关；关闭后提醒尽量以静音通知展示。
- `dailyGuidanceEnabled: boolean`，是否显示每日提示，默认 `true`。
- `dailyGuidanceStyle: "calm" | "practical" | "encouraging"`，每日提示风格，默认 `"practical"`；仅影响客户端内置文案选择，不保存或上传任何任务内容。

## 兼容规则
- 当前开发阶段不迁移旧 `todo-data.json`。
- 前端 normalize 仍会为缺失字段补默认值，保证 SQLite 读取出的局部数据能稳定进入 UI。
- `comments` 和 `editorMode` 保留在前端结构中，避免后续迁移或导入时丢失历史字段。
- 删除任务先复制到 `trash`，永久删除才从 `tasks` 和 `trash` 移除。
- 删除清单先复制到 `listTrash`，清单下任务进入 `trash` 并标记 `deletedByListId`；恢复清单时一并恢复这些任务。
- 垃圾桶数据按 `settings.trashRetentionDays` 自动清理过期项。
- 前端 normalize 会为旧设置补齐提醒通知相关字段，默认开启系统提醒和提醒声音。
- 前端 normalize 会为旧设置补齐每日提示字段；这是已有 `settings` JSON 的默认值补齐，不改变 SQLite 表结构，也不需要数据库结构迁移。
- v6 → v7 的个人资料迁移会为旧数据创建一个本机 `Profile`：生成 UUID、昵称设为 `"易简用户"`、头像字段为 `null`、`accountId` 为 `null`；不得要求用户登录或覆盖既有任务数据。
- 前端会为旧任务补齐 `reminderNotifiedAt: null`；应用运行期间由统一调度器在提醒时间发送通知，重启或从休眠恢复后会补发尚未投递的提醒一次。
- 用户可创建本机恢复点。每个恢复点位于应用数据目录的 `backups/<snapshot-id>/`，包含 `simpletodo.db`、`attachments/`、`profile/` 和 `backup.json`；恢复时先创建 `before-restore` 安全点，恢复失败会尝试回滚当前数据。
- v7 → v8 会为旧任务补齐 `reminderDisabled: false`；已有截止日期但未单独设置提醒的任务，会在截止时间自动提醒。
- 旧数据中 `taskGroups` 为空数组，`task.taskGroupId` 默认为 `null`，`list.viewMode` 默认为 `"list"`。

## 数据迁移

### 迁移处理器

数据版本迁移由专用模块 `src/utils/migrator.js` 负责，支持跨版本升级。

| 版本 | 迁移函数 | 变更内容 |
|------|----------|----------|
| v1 → v2 | `migrateV1ToV2()` | 基础结构升级，添加 groups、viewOrders、settings 等字段 |
| v2 → v3 | `migrateV2ToV3()` | 添加 `taskGroups` 数组、`list.viewMode`、`task.taskGroupId` |
| v3 → v4 | `migrateV3ToV4()` | 任务分组颜色规范化，添加 `TaskGroup.color` 字段验证 |
| v4 → v5 | `migrateV4ToV5()` | 添加分组模式已完成任务的展示设置，保留任务原有 `taskGroupId`、完成状态和完成时间 |
| v5 → v6 | `migrateV5ToV6()` | 为任务和垃圾桶任务补齐提醒投递状态 `reminderNotifiedAt` |
| v6 → v7 | `migrateV6ToV7()` | 添加本机个人资料 `profile`，为昵称和头像提供默认值 |
| v7 → v8 | `migrateV7ToV8()` | 添加任务级提醒关闭状态，截止日期默认作为提醒时间 |

### 跨版本升级

当用户从旧版本（如 v1）直接升级到新版本（如 v5）时，迁移器会按顺序执行所有中间版本的迁移：

```
v1 → v2 → v3 → v4 → v5
```

每个迁移函数只依赖于前一个版本的数据结构，确保跨版本升级的安全性。

### 调用时机

应用启动加载数据时，`loadData()` 会：
1. 创建原始数据备份
2. 调用 `migrateData()` 自动检测并执行迁移
3. 调用 `validateData()` 验证迁移后的数据完整性
4. 验证通过后立即持久化迁移结果；验证或迁移失败时阻止写入，并提示用户恢复或升级应用

```js
import { migrateData, validateData, createBackup } from '@/utils/migrator'

// 1. 创建备份
const backup = createBackup(rawData)

// 2. 执行迁移
const migrated = migrateData(rawData)

// 3. 验证数据
const { valid, errors } = validateData(migrated)
if (!valid) {
  // 降级处理
}
```

### 错误处理

- 迁移前将原始状态备份到应用数据目录的 `backups/`；Web 模式保存到 localStorage。
- 缺少中间迁移、数据校验失败或读取到未来版本时，停止加载后的自动保存，避免覆盖原始数据。
- 数据版本高于当前应用支持范围时，要求升级应用，绝不降级写回。

### 扩展规范

新增版本迁移时：
1. 在 `migrations` 对象中添加迁移函数
2. 递增 `CURRENT_VERSION`
3. 在 `getSupportedVersions()` 中添加新版本号
4. 编写迁移逻辑（纯函数，可测试）
5. 添加对应的数据验证规则

## 实体关系图

```
┌─────────────────────────────────────────────────────────────────┐
│                           Root                                  │
│  schemaVersion: 8                                               │
└─────────────────────────────────────────────────────────────────┘
         │
         ├── groups: Group[] ──────────────────────────────────────┐
         │                                                          │
         ├── lists: List[] ────────────────────────────────────────┐│
         │                                                          ││
         ├── taskGroups: TaskGroup[] ─────────────────────────────┐││
         │                                                        │││
         ├── tasks: Task[] ──────────────────────────────────────┐│││
         │                                                        ││││
         ├── trash: Task[]                                       ││││
         │                                                        ││││
         ├── listTrash: ListTrash[]                              ││││
         │                                                        ││││
         ├── viewOrders: Record<string, string[]>                ││││
         │                                                        ││││
         └── settings: Settings                                  ││││
                                                                 ││││
┌────────────────────────────────────────────────────────────────┐││││
│                        Group（侧栏分组）                       ││││
│  id: string                                                    ││││
│  name: string                                                  ││││
│  collapsed: boolean                                            ││││
│  sortOrder: number                                             ││││
└────────────────────────────────────────────────────────────────┘││││
         │                                                       ││││
         │ 1:N (groupId)                                        ││││
         ▼                                                       ││││
┌────────────────────────────────────────────────────────────────┐││││
│                        List（清单）                            ││││
│  id: string                                                    ││││
│  name: string                                                  ││││
│  groupId: string | null  ◄─────────────────────────────────────┘│││
│  color: string                                                 ││││
│  isSystem: boolean                                             ││││
│  sortOrder: number                                             ││││
│  viewMode: "list" | "group"                                    ││││
└────────────────────────────────────────────────────────────────┘││││
         │                                                       ││││
         │ 1:N (listId)                                          ││││
         ▼                                                       ││││
┌────────────────────────────────────────────────────────────────┐││││
│                    TaskGroup（任务分组）                        ││││
│  id: string                                                    ││││
│  name: string                                                  ││││
│  listId: string ───────────────────────────────────────────────┘│││
│  sortOrder: number                                             ││││
│  collapsed: boolean                                            ││││
│  createdAt: string                                             ││││
│  updatedAt: string                                             ││││
└────────────────────────────────────────────────────────────────┘││││
         │                                                       ││││
         │ 1:N (taskGroupId)                                     ││││
         ▼                                                       ││││
┌────────────────────────────────────────────────────────────────┐││││
│                        Task（任务）                            ││││
│  id: string                                                    ││││
│  title: string                                                 ││││
│  listId: string ───────────────────────────────────────────────┘│││
│  taskGroupId: string | null ───────────────────────────────────┘││
│  completed: boolean                                            │││
│  pinned: boolean                                               │││
│  important: boolean                                            │││
│  myDayDate: string | null                                      │││
│  dueDate: string | null                                        │││
│  reminderAt: string | null                                     │││
│  repeatRule: string | null                                     │││
│  priority: 0 | 1 | 2 | 3                                      │││
│  tags: string[]                                                │││
│  subtasks: Subtask[] ──────────────────────────┐               │││
│  attachments: Attachment[] ─────────────────┐  │               │││
│  ...                                       │  │               │││
└─────────────────────────────────────────────┼──┼───────────────┘││
                                              │  │               ││
                                              ▼  ▼               ││
┌──────────────────────┐  ┌──────────────────────┐               ││
│    Subtask（子任务）  │  │  Attachment（附件）   │               ││
│  id: string          │  │  id: string           │               ││
│  title: string       │  │  kind: string         │               ││
│  completed: boolean  │  │  mime: string         │               ││
│  sortOrder: number   │  │  originalName: string │               ││
└──────────────────────┘  │  relativePath: string │               ││
                          │  sha256: string       │               ││
                          │  sizeBytes: number    │               ││
                          │  url: string          │               ││
                          └───────────────────────┘               ││
                                                                  ││
┌────────────────────────────────────────────────────────────────┐││
│                    ListTrash（清单回收站）                      ││
│  id: string（原清单ID）                                        ││
│  name: string                                                  ││
│  groupId: string | null  ◄─────────────────────────────────────┘│
│  color: string                                                 │
│  deleted: true                                                 │
│  deletedAt: string                                             │
│  taskCount: number                                             │
└────────────────────────────────────────────────────────────────┘

关系说明：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
• Group 1:N List        一个侧栏分组可包含多个清单
• List 1:N TaskGroup    一个清单可创建多个任务分组（分组模式）
• List 1:N Task         一个清单包含多个任务
• TaskGroup 1:N Task    一个任务分组包含多个任务（分组模式）
• Task 1:N Subtask      一个任务可有多个子任务
• Task 1:N Attachment   一个任务可有多个附件
• List → ListTrash      删除清单时移入回收站
• Task → Trash          删除任务时移入回收站
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

视图模式说明：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
• list 模式：平铺显示所有任务，忽略 taskGroupId
• group 模式：按 TaskGroup 分组显示，未分组任务单独显示
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
