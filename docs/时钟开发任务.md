# 专注与节律产品开发任务

本文把产品规划拆成可交付、可测试、可验收的任务。状态以实际代码和验证结果为准；每完成一项，都要记录遗留问题并判断是否进入下一轮优化。

## 总体验收门槛

- 本机离线可完成专注、节律提醒、暂停/恢复、回顾和数据删除；旧数据库升级后任务数据不丢失。
- Windows 与 macOS 都提供等价的计时、通知、后台恢复和权限反馈；不能等价实现时，界面明确显示降级行为。
- 不记录键入内容、鼠标轨迹或前台应用名称；不作医疗效果承诺；用户始终可以暂停、延后或关闭提醒。
- 所有界面入口可用、可禁用或给出明确说明；任何上线功能通过 `npm run build`，涉及 Rust 改动还通过 `npm run tauri build` 或记录无法构建的原因。

## T1：时钟核心与本地数据基础

**状态：功能完成，等待跨场景验收**

### 交付内容

- [x] 数据模型 v10：专注预设、当前会话、会话历史和时钟设置；旧 JSON 自动补默认值。
- [x] 专注状态机：番茄、深度专注、自由计时；开始、暂停、继续、完成、提前结束、短/长休息与跳过休息。
- [x] 关联未完成任务或无任务专注；结束记录“完成 / 放弃 / 中断”及可选备注。
- [x] 当前会话、预设和历史写入既有 SQLite 保存路径，重启后能恢复正确剩余时间或暂停状态。
- [x] 初版专注工作台替换时钟占位页。
- [x] 补齐短休息/长休息阶段、跳过休息、编辑个人预设和结束备注。

### 测试

- [ ] 自动化：迁移 v9 → v10、空数据初始化、非法会话字段归一化（现有项目尚未配置单元测试脚本）。
- [ ] 手工：开始 1 分钟专注后刷新/重启，剩余时间按真实时间变化；暂停后重启，剩余时间不变。
- [ ] 手工：关联任务、解除关联、完成/放弃/中断均留下正确历史且不改变任务完成状态。
- [x] 构建：`npm run frontend:build`、`cargo check`。
- [ ] 构建：`npm run tauri build` 完整打包验证。

### 验收标准

1. 用户在 3 次点击内可开始默认专注；运行、暂停和结束状态都可见且互斥。
2. 时钟不依赖前端页面常驻；从后台、睡眠恢复或重启后，显示由保存的时间点计算的真实状态。
3. 保存失败时不清空会话或历史，并提示用户数据未保存。

### 优化复核

- 检查倒计时刷新是否造成不必要重渲染、不同模式是否容易理解、任务选择是否阻塞开始；必要时增加最近任务入口或缩短操作路径。

## T2：节律提醒引擎与模板

**状态：进行中**

### 交付内容

- [x] 提醒实体与首批模板已收敛为六项：护眼远望、补水、起身活动、肩颈舒展、呼吸放松、今日收尾；用户仍可自定义。
- [x] 间隔、固定时刻模型；工作日、工作时段、完成/延后/跳过/跳过当天和全局暂停。
- [x] 提醒中心、编辑表单与模板启停。连续活跃时长、静默时段和系统通知将在 T3 接入原生能力后完成。

### 测试与验收

- [ ] 每种触发器在边界时间、跨午夜、静默期、延后和跳过当天后的下一次触发均正确。
- [ ] 两条同时到点时按队列呈现且不丢失；关闭权限时保留提醒设置并说明未投递原因。
- [ ] 用户可以在不删除数据的情况下暂停全部提醒或恢复任意单条提醒。

### 优化复核

- 检查模板初始值是否过多、提示是否干扰专注；依据实际测试调整默认频率、文案与通知层级。

### T2.1：节律计时语义与多提醒队列重构

**状态：计划中，先完成产品与交互验收，再替换现有调度逻辑**

#### 目标

- 解决“右侧开启多项、左侧只显示一项”缺少解释的问题，让用户始终知道每项提醒是在倒计时、累计活跃、待处理还是暂停。
- 将间隔提醒定义为从开启/恢复或上次处理后开始的倒计时；将“久坐”改为诚实的连续电脑活跃时长模型，不把短暂无输入误认作已完成休息。
- 以提醒队列和 5 分钟碰撞窗口控制打扰，而不是让多项规则同时弹出或静默丢失。

#### 交付内容

- [ ] 将提醒运行态从仅靠 `lastCompletedAt` 推导，升级为可持久化的 `cycleStartedAt`、`nextDueAt`、`pendingSince`、`lastResolvedAt` 和状态枚举；旧数据迁移后从启用时间安全初始化，不触发即时补发。
- [ ] 提醒主卡显示“倒计时 / 累计进度 / 固定时刻”三种明确样式；补充“接下来”队列，列出后续两项的真实状态与下次时刻。
- [ ] 提醒模板卡增加下次触发信息；停用项不再展示看似运行中的开关状态。首次启用与修改频率后立即给出下一次提醒时刻。
- [ ] 将同时运行上限设为 3 项，并实现替换提示和低打扰组合建议；已有超过上限的数据只读保留，待用户主动收敛，不自动关闭。
- [ ] 引入同到期碰撞窗口、优先级、待处理队列和单次通知抑制；完成、延后、跳过本次、跳过当天后的下一次触发均可预测。
- [ ] 连续活跃模式新增“真实离席阈值”与手动“我已活动”重置；界面和帮助文本明确隐私边界与平台降级。
- [ ] 评估将节律的下一次触发移入 Rust 原生调度，以覆盖隐藏窗口、睡眠恢复与 WebView 定时器停摆；在实现前定义 Windows/macOS 的等价回退行为。

#### 验收场景

- [ ] 开启补水（60 分钟）、护眼（20 分钟）与站立（45 分钟）后，用户可同时看到三项的下次时刻；大卡只突出最近项，但不再隐藏其他两项。
- [ ] 间隔提醒的进度与“还有 X 分钟 / 已过 Y 分钟”一致；完成、跳过本次、暂停后重开应用，下一次时刻仍正确。
- [ ] 连续活跃 45 分钟到期后，短暂无输入 30 秒不重置；真实离席达到阈值或用户手动确认活动后才开始下一轮。
- [ ] 两项在 5 分钟内同时到期时，只出现一个前台提醒；另一项进入可见队列，当前项处理完后可继续处理，且没有漏提醒或连环轰炸。
- [ ] 第 4 项启用时不静默加入运行组；用户能明确选择替换哪一项或返回调整。

## T3：原生活跃时长、通知和后台体验

**状态：进行中**

### 交付内容

- [x] Windows 原生空闲时长 command；连续空闲达到默认 3 分钟才视为自然离席并重置累计活跃时间。macOS/Web 明确降级为不支持该触发器。
- [x] 节律提醒的系统通知与应用内非阻塞提示；未授权时保留设置并静默回退。
- [x] 专注/休息截止由 Rust 后台调度；前台显示应用内完成弹层，最小化、隐藏或失焦时发送系统通知，并支持专用测试通知。
- [x] 运行中可打开桌面专注控制器，实时暂停/继续、增减 5 分钟、完成本轮并独立切换置顶；控制器持续可见，临时提醒通过非抢占优先级队列依次展示，并自动避让控制器。
- [ ] 托盘/菜单栏快捷操作与节律提醒权限状态。
- [ ] 手动专注模式、全屏/演示/系统勿扰的可理解处理。

### 测试与验收

- [ ] Windows/macOS 分别验证最小化后台、锁屏、睡眠、恢复、通知拒绝、多显示器和全屏场景。
- [ ] 活跃时长不可用时明确改为墙上时间或禁用，绝不声称已检测键鼠活跃度。
- [ ] 不采集或持久化任何原始输入、轨迹或应用名称。

### 优化复核

- 测试通知是否在会议或专注时打断用户；评估需不需要调整延后限制、预告和浮层样式。

## T4：完整工作台、引导、回顾和数据控制

**状态：进行中**

### 交付内容

- [ ] 迷你悬浮计时器、休息界面、声音/动效/无障碍设置、首次引导。
- [x] 今日与本周专注回顾、专注结果备注、单条删除和经确认的当天记录清理。
- [x] 回顾页统一展示专注与节律历史；新专注采集暂停/继续等过程时间线，节律处理记录响应结果与耗时，单条记录可展开详情。
- [ ] 按任务/提醒的组合筛选、记录保留期和恢复点联动。
- [ ] 使用指南、隐私说明、错误恢复与数据迁移说明。

### 测试与验收

- [ ] 键盘可以完成核心流程；辅助技术能读取阶段、剩余时间、提醒原因和操作结果。
- [ ] 删除单条、按日期范围、按提醒删除均不影响任务数据；恢复点可恢复时钟记录。
- [ ] 统计与原始记录可相互核对，跨日、夏令时与系统时钟变化有可解释结果。

### 优化复核

- 以真实任务流走查“开始专注—被提醒—回顾”的连续体验，处理信息层级、空状态、性能和视觉一致性问题。

## 持续优化机制

每个任务完成后执行以下闭环：

1. 运行自动构建与相关手工场景，记录结果。
2. 对照验收标准逐项标记通过、失败或有条件通过。
3. 检查错误日志、保存/恢复路径、无障碍、跨平台差异和不必要的权限/数据收集。
4. 将影响核心闭环的缺陷回填到当前任务；可选增强进入后续任务，不以牺牲可靠性换取功能数量。
5. 更新本文、产品文档和使用指南，提交一个只包含该稳定功能的中文 commit。
